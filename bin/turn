#!/usr/bin/env ruby

require_relative '../lib/turn'

board = [" ", 
 
def turn(board)
  puts "Please enter 1-9:"
  input = gets.strip
  index = input_to_index(input)
  if valid_move?(board,index)
    move(board,index)
  else
end
